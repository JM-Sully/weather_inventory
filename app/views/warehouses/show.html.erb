<h1><%= @warehouse.name %></h1>
<p><%= @warehouse.city %>, <%= @warehouse.country %></p>
<%= link_to "Edit", edit_warehouse_path(@warehouse) %>
<%= link_to "Destroy", warehouse_path(@warehouse), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %>

<p><%= link_to "View all Warehouses", warehouses_path %></p>
